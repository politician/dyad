/**
 * Stub implementation of search/replace markers
 * Maintains minimal compatibility after pro feature removal
 */

export function escapeSearchReplaceMarkers(text: string): string {
  // Simple stub - just return the text as-is
  return text;
}
